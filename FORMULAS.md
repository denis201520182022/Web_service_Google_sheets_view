# 📐 Работа с формулами в Zabota Tables

## Основы

Все формулы начинаются с знака `=`

## 🔢 Арифметические операции

```
=A1+B1          Сложение
=A1-B1          Вычитание
=A1*B1          Умножение
=A1/B1          Деление
=A1^2           Возведение в степень
```

## 📊 Базовые функции

### Суммирование
```
=SUM(A1:A10)         Сумма диапазона
=SUM(A1,B1,C1)       Сумма отдельных ячеек
=SUM(A:A)            Сумма всего столбца
```

### Среднее значение
```
=AVERAGE(A1:A10)     Среднее арифметическое
=AVERAGEIF(A1:A10,">5")  Среднее с условием
```

### Подсчет
```
=COUNT(A1:A10)       Количество чисел
=COUNTA(A1:A10)      Количество непустых ячеек
=COUNTIF(A1:A10,">5") Подсчет с условием
```

### Минимум/Максимум
```
=MIN(A1:A10)         Минимальное значение
=MAX(A1:A10)         Максимальное значение
```

## 📝 Текстовые функции

```
=CONCATENATE(A1," ",B1)  Объединение текста
=LEFT(A1,3)              Первые 3 символа
=RIGHT(A1,3)             Последние 3 символа
=LEN(A1)                 Длина текста
=UPPER(A1)               В верхний регистр
=LOWER(A1)               В нижний регистр
=TRIM(A1)                Удаление пробелов
```

## 🔍 Логические функции

```
=IF(A1>5,"Да","Нет")     Условие
=AND(A1>5,B1<10)         Логическое И
=OR(A1>5,B1<10)          Логическое ИЛИ
=NOT(A1>5)               Логическое НЕ
```

## 📅 Дата и время

```
=TODAY()                 Сегодняшняя дата
=NOW()                   Текущая дата и время
=YEAR(A1)                Год из даты
=MONTH(A1)               Месяц из даты
=DAY(A1)                 День из даты
```

## 🎯 Поиск и ссылки

```
=VLOOKUP(A1,B:C,2,FALSE) Вертикальный поиск
=HLOOKUP(A1,B:C,2,FALSE) Горизонтальный поиск
=INDEX(A:A,5)            Значение по индексу
=MATCH(A1,B:B,0)         Позиция значения
```

## 📈 Примеры использования

### Пример 1: Таблица продаж
```
A          B        C           D
Товар      Цена     Количество  Сумма
Ноутбук    50000    2           =B2*C2
Мышка      500      5           =B3*C3
                    ИТОГО:      =SUM(D2:D3)
```

### Пример 2: Средний балл
```
A          B    C    D    E
Ученик     М1   М2   М3   Средний
Иван       5    4    5    =AVERAGE(B2:D2)
Мария      4    5    5    =AVERAGE(B3:D3)
```

### Пример 3: Условное форматирование
```
A          B              C
Баллы      Результат
85         =IF(A2>=80,"Отлично",IF(A2>=60,"Хорошо","Плохо"))
55         =IF(A3>=80,"Отлично",IF(A3>=60,"Хорошо","Плохо"))
```

### Пример 4: Работа с текстом
```
A              B              C
Имя            Фамилия        Полное имя
Иван           Петров         =A2&" "&B2
Мария          Сидорова       =A3&" "&B3
```

## 🔗 Ссылки между листами

```
=Лист2!A1              Ячейка с другого листа
=SUM(Лист2!A1:A10)     Диапазон с другого листа
```

## ⚠️ Частые ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| #DIV/0! | Деление на ноль | Проверь делитель |
| #VALUE! | Неверный тип данных | Проверь формулу |
| #REF! | Ссылка на удаленную ячейку | Исправь ссылку |
| #NAME? | Неизвестное имя функции | Проверь название |
| #N/A | Значение не найдено | Проверь данные |

## 💡 Советы

1. **Абсолютные ссылки**: `=$A$1` (не изменяется при копировании)
2. **Смешанные ссылки**: `=$A1` или `=A$1`
3. **Названные диапазоны**: можно использовать имена вместо ссылок
4. **Вложенные функции**: `=IF(SUM(A1:A10)>100,"Да","Нет")`

## 📚 Полный список функций Google Sheets

https://support.google.com/docs/table/25273

---

**Zabota Tables поддерживает все формулы Google Sheets!**